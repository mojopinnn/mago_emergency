# 🚀 MAGO Emergency - GCP Cloud Run 배포 & 환경변수 관리 통합 가이드
> 이 문서는 다른 앱(새로운 Repository)을 Google Cloud Run에 최초 배포하고 지속적으로 수정 재배포하는 방법과, Cline과 협업(바이브 코딩) 시 민감한 Key 정보를 안전하게 격리하고 환경변수로 일관되게 관리하기 위한 통합 마스터 가이드라인입니다.

---

## 📂 1. 배포 관련 핵심 파일 및 역할

GCP Cloud Run 환경에 서비스를 안전하고 빠르게 배포하기 위해 프로젝트 최상위 경로에 배치해야 할 필수 파일들입니다.

| 파일명 | 역할 | 상세 설명 |
| :--- | :--- | :--- |
| **`Dockerfile`** | **컨테이너 설계도** | 소스코드를 격리된 독립 환경(Container)으로 패키징하기 위해 필요한 빌드 절차를 규정합니다. (GCP가 이를 읽어서 서버 이미지를 원격 빌드함) |
| **`deploy_gcp.sh`** | **최초 구축 및 직접 배포 스크립트** | 구글 클라우드 환경(API 활성화, Docker 이미지 저장소 개설 등)을 자동으로 구축하고 개발자 환경에서 직접 빌드 및 배포를 명령하는 비상용/세팅용 도구입니다. |
| **`deploy_mago.sh`** | **자동 배포 트리거 스크립트** | 작업 공간(코드스페이스)의 변경 사항 중 필요한 폴더만 쏙 분리하여 깃허브 배포 전용 레포지토리로 강제 깃푸시(`force push`)해 줍니다. (이 업로드 행위가 구글의 배포 트리거를 작동시킵니다.) |
| **`.dockerignore`** | **빌드 제외 필터** | `node_modules`나 로컬 비밀 키값(`.env`) 등 배포 컨테이너 이미지에 불필요하게 포함되거나 보안에 민감한 파일 목록을 정의합니다. |

---

## ⚙️ 2. GCP 콘솔 사전 준비 사항 & 주의사항

배포 스크립트를 작동시키기 전에 **Google Cloud Web Console**에서 선행되어야 할 수동 절차들입니다.

### ① 결제 정보 등록 (필수)
* Google Cloud Run 및 Cloud Build를 사용하려면 **반드시 결제 계정(신용카드 정보)이 GCP 계정에 등록**되어 있어야 합니다.
* 개인 토이 프로젝트 수준(Free Tier 범위)에서는 요금이 청구되지 않거나 미미한 수준에 불과하지만, 결제 수단이 등록되지 않으면 구글 클라우드가 API 빌드 기능을 강제로 차단하므로 무조건 선행 등록해야 합니다.

### ② GCP 프로젝트 생성 및 ID 확인
* GCP 콘솔 상단에서 새로운 프로젝트를 수동으로 생성하고 발급된 고유의 **`Project ID`**(예: `my-awesome-app-12345`)를 복사해 둡니다. (스크립트 실행 시 필요)
* 프로젝트 생성 시, 이전에 등록한 기존 결제 계정(신용카드 정보)을 공유 연동해 줍니다. (새 프로젝트마다 카드를 새로 입력할 필요 없이 마우스 클릭 한 번으로 공유 연동 가능)

---

## 🔐 3. 효율적인 Key 관리 및 일관된 코딩 규칙 (Cline 협업 필수 규칙)

Gemini API Key, 결제 정보, ShotGrid API Key와 같은 민감한 보안 키들은 **절대 소스코드에 하드코딩하여 깃허브에 노출하면 안 됩니다.** 

GCP 환경에서 가장 권장되며, Cline과 함께하는 바이브 코딩 시 늘 통일해야 할 Key 관리 규칙입니다.

### ① 개발자 로컬 PC / 코드스페이스 환경
* 실제 비밀번호와 API 키값은 오직 로컬 환경의 `.env` 파일에만 적어두며, 이 파일은 절대로 깃허브 저장소에 올라가선 안 됩니다. (반드시 `.gitignore`에 등록하여 관리)
* 저장소(Repository)에는 실제 키값을 지운 **`.env.example`** 양식 파일만 공유하여 템플릿 형태로 제공합니다.

### ② GCP Cloud Run 실서버 환경 (환경변수 이용)
* 코드가 구글 클라우드에 배포되면 웹 콘솔(GCP Console)의 **환경변수(Environment Variables)**에 직접 키값을 등록해 둡니다.
* 배포가 완료되면 서버 소스코드가 구동될 때 클라우드가 주입한 키를 자동으로 안전하게 읽어 들입니다. (GCP 콘솔 ➡️ Cloud Run ➡️ 서비스 선택 ➡️ [수정 및 새 버전 배포] ➡️ [컨테이너] 탭 ➡️ [환경변수] 영역에서 추가 가능)

---

## 🤖 4. Cline 바이브 코딩 연동: `.clinerules` 활용법

Cline이 사용자님의 지시 없이도 **항상 100% 동일한 고품질의 보안 및 키 관리 규격**을 지키며 자동으로 환경변수를 통일하도록 강제하는 방법입니다.

프로젝트 최상위 경로에 **`.clinerules`** 파일을 만들고 아래 룰을 고정해 두면, 모든 코드가 자동으로 시스템 환경변수를 타도록 안전하게 개발됩니다.

```markdown
# [Rule] Unified Environment Variables & Key Management Rule for Cline

## 1. 절대 규칙 (Never Violate)
- 소스코드 내부에 API Key, 패스워드, 외부 URL, 포트 번호 등 민감한 정보(Secrets)를 직접 입력(Hardcoding)하는 것을 절대 금지한다.
- 실물 정보는 무조건 시스템 환경변수(Environment Variables)로부터 동적으로 읽어오도록 코드를 구현한다.

## 2. 언어별 코딩 표준 (Coding Standard)
- **Node.js (JavaScript / TypeScript)**:
  - 중요 환경 변수는 반드시 `process.env.VARIABLE_NAME` 형식을 통해 참조한다.
  - 외부 서비스 연결 시 포트는 항상 `process.env.PORT || 8080`과 같이 동적 할당되도록 처리한다.
- **Python**:
  - 중요 환경 변수는 반드시 `os.environ.get("VARIABLE_NAME")` 또는 `os.getenv("VARIABLE_NAME")` 형식을 사용한다.

## 3. 파일 및 저장소 관리 규칙 (Git & File Rule)
- 실물 키값이 들어가는 `.env` 파일은 절대 Git 저장소에 커밋하지 않는다. (무조건 `.gitignore`에 등록할 것)
- 새로운 환경변수를 도입할 경우, 반드시 실제 키값이 비어 있는 `.env.example` 파일을 세트로 작성하거나 업데이트하여 다른 개발자가 구조를 파악할 수 있게 한다.

## 4. 배포 연동 (Deployment Integration)
- 새로운 환경변수가 코드에 추가되는 즉시, 배포 스크립트(`deploy_gcp.sh` 또는 Dockerfile 등)에도 해당 변수를 주입하는 코드를 자동으로 함께 반영한다.
```

---

## 📝 5. 단계별 실전 배포 절차 (최초 배포 vs 수정 재배포)

### 📌 [A 단계] 최초 1회 배포 및 뼈대 구축 (First-time Build & Deploy)
새로운 저장소(Repository)를 만들어 앱을 처음 구글 서버에 올릴 때 거치는 절차입니다.

#### 1단계: gcloud CLI 설치 및 구글 계정 로그인
```bash
# 구글 계정 로그인 창이 뜨면 배포할 구글 계정으로 로그인합니다.
gcloud auth login
```

#### 2단계: `deploy_gcp.sh` 최초 실행
```bash
# 실행 권한 부여 후 실행
chmod +x deploy_gcp.sh
./deploy_gcp.sh
```
* 스크립트 안내에 따라 **GCP Project ID**, **배포 리전**, **서비스 이름**, **각종 API Key**들을 입력하면 구글 서버에 최초로 코드가 빌드되어 첫 번째 실배포 URL이 발급됩니다.

#### 3단계: GCP 웹 콘솔에서 깃허브 자동 배포 연동하기 (CI/CD)
1. GCP 콘솔의 **Cloud Run** 페이지로 이동하여 생성된 서비스를 클릭합니다.
2. 상단의 **[연속 배포 설정(SET UP CONTINUOUS DEPLOYMENT)]**을 누릅니다.
3. 제공업체로 **GitHub**를 선택하고 계정을 인증한 뒤, 배포 대상 **Repository**와 **브랜치(예: main)**를 선택합니다.
4. 빌드 구성 유형으로 **`Dockerfile`**을 선택하고 경로를 `/Dockerfile`로 입력한 뒤 저장합니다.
5. 이제 깃허브와 구글 클라우드가 완벽히 연동되었습니다.

---

### 📌 [B 단계] 일상적인 수정 및 재배포 과정 (Daily Update & Redeploy)
첫 배포 이후 코드스페이스에서 버그를 고치거나 새로운 기능을 추가하여 실시간으로 서버에 적용하는 간편한 일상 절차입니다.

#### 1단계: 작업 공간(코드스페이스)에서 자유롭게 코드 수정
* `.clinerules` 지침에 따라 안전하게 키값을 변수로 감싸 코딩 작업을 수행합니다.

#### 2단계: `deploy_mago.sh` 실행 (자동 배포 트리거)
```bash
# 깃푸시 및 배포 트리거 단 한 줄 실행
bash deploy_mago.sh
```
* **동작 완료:** 이 명령어 하나로 배포 브랜치에 자동 깃푸시가 완료되고, 이를 실시간 감시 중이던 구글 클라우드가 이벤트를 수신하여 약 1~2분 내에 자동으로 새로운 버전의 서비스를 업데이트 빌드 및 실배포 완료합니다!

---

## ⚠️ 6. 배포 시 핵심 주의사항 (실패 방지 체크리스트)

1. **포트(Port) 동적 할당 문제**:
   * Google Cloud Run은 실행될 때 컨테이너 내부 웹 서버에 환경 변수 `PORT`를 주입합니다.
   * 소스코드 내 웹 서버 포트 지정 시, 특정 숫자로 하드코딩하지 마시고 항상 **`process.env.PORT || 8080`** 형태로 동적 바인딩되도록 작성해야 배포 실패(포트 충돌 및 타임아웃)를 방지할 수 있습니다.
2. **패키지 매니저의 일치**:
   * 프로젝트 빌드 시 사용되는 실제 잠금 파일(예: `pnpm-lock.yaml`)과 `Dockerfile` 내의 빌드 명령어(예: `pnpm install`, `pnpm run build`)가 완벽히 일치해야 빌드 오류를 방지할 수 있습니다.
3. **루트 폴더(`PREFIX`) 설정**:
   * 모노레포 환경인 경우 `deploy_mago.sh` 내부의 `PREFIX` 변수가 배포 대상 서브폴더 경로를 알맞게 지목하고 있어야 합니다. (만약 모노레포가 아닌 단일 저장소 구조라면 별도의 subtree 분리 없이 일반적인 `git push origin main`만으로 자동 배포 연동이 훨씬 수월합니다.)
